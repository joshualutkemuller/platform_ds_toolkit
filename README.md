# platform_ds_toolkit
Reusable Python library repo: Platform DS Toolkit — modular IO, archiving, lightweight data checks, pipeline/task runner (retries + timing), SMTP notifications (dry-run by default), plus optional SQL + HTTP helpers.
